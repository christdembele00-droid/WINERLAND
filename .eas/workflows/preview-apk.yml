name: Create Preview Android APK

on:
  push:
    branches: ['main']

jobs:
  build_android:
    name: Build Preview Android APK
    type: build
    params:
      platform: android
      profile: preview
